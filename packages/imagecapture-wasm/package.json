{
  "name": "imagecapture-wasm",
  "version": "1.0.0",
  "description": "WASM compilation + WASM TS types",
  "scripts": {
    "build": "mkdir -p dist && touch dist/dummy.wasm",
    "clean": "rimraf .turbo dist *.tsbuildinfo"
  },
  "files": [
    "dist",
    "types"
  ],
  "types": "./types/index.ts",
  "main": "./types/index.ts",
  "module": "./types/index.ts",
  "devDependencies": {
    "@types/emscripten": "^1.39.6",
    "concurrently": "^8.0.1",
    "dotenv-cli": "^7.2.1",
    "rimraf": "^5.0.0",
    "tsconfig": "workspace:*",
    "tsx": "^3.12.7",
    "typescript": "^5.0.4",
    "vite": "^4.3.6",
    "zod": "^3.21.4",
    "zx": "^7.2.2"
  }
}
